//Initializing my variables for radius, x, and y.
let r = 10;
let x = r * Math.sin(mouseX);
let y = r * Math.cos(mouseX);
let res = {}
//Setting up the Canvas and Background for my circle to be drawn.
function setup() {
    createCanvas(800, 600);
    background('black');
}
//Translate lines set up before the draw functions.
translate(100,00);

//Setting up the function draw().
function draw() {

}
//Setting up the polarPoint() function.
function polarPoint() {
    
}
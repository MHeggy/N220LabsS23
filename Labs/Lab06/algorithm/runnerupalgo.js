//We are to create an array with the 3 "best times" (numbers).
//We need to write the array so the times are displayed in order.
//There will be an HTML document used here with three separate divs.
//<div id="winner"></div>
//<div id="second"></div>
//<div id="third"></div>
//Now to the JS code...
//let divs = [document.getElementById("winner"), document.getElementById("second"), document.getElementById("third")];
//for loop
//for(i = 1; i <= 3, i++) {
    //divs[0].innerHTML = "this is the winner!";
    //divs[1].innerHTML = "this is the second place!";
    //divs[2].innerhTML = "third place, better luck next time!";
//}